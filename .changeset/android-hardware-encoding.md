---
"expo-device-hub": minor
---

Add optional hardware H.264 encoding for Android gRPC streams. Select it with `--encoder hardware` or the Encoder control in Stream options, with active encoder details and actionable hardware errors. Software encoding remains the default.

---
"@expo/hub-client": minor
---

Bring the serve-sim device client in line with `@expo/serve-sim` 0.3.0: typed exec-ws host actions instead of shell commands (app details, simulated location), the current hardware-keyboard protocol, the WebRTC first-frame grace period, scroll-wheel forwarding as native scrolls, queued input across reconnects, and phone-keyboard capture helpers (`KeyboardCapture`, `sendKeyEvents`).

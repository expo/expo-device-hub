---
'@expo/hub-client': patch
---

Emit declaration files and runtime files with explicit `.js` import extensions, so TypeScript consumers with `moduleResolution: node16` or `nodenext` can resolve the exported types.

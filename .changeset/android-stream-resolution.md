---
'expo-device-hub': minor
---

Add runtime Android stream resolution and capture-source controls, use the 60 FPS capture default, expose pipeline diagnostics, and accurately count browser-presented frames while preserving the Hub session across stream restarts.

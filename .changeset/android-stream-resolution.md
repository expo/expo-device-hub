---
'expo-device-hub': minor
---

Add runtime Android stream resolution and capture-source controls, use the 60 FPS capture default, and expose pipeline diagnostics while preserving the Hub session across stream restarts.

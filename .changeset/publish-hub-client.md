---
'@expo/hub-client': minor
---

Publish `@expo/hub-client` to npm as a standalone package. The package is no longer private, ships a README and LICENSE, leaves its tests out of the tarball, and emits ESM with explicit `.mjs` import extensions so Node can resolve it.

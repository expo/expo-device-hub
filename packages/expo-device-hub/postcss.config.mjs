/**
 * Tailwind v4 via PostCSS — same integration as the expo-agent web app.
 * @see https://tailwindcss.com/docs/configuration#using-postcss
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
